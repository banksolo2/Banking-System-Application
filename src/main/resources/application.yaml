spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/banking_app_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  mail:
    host: smtp.gmail.com
    port: 587
    username: bankstechhrms@gmail.com
    password: gwbzvgokvfbnhkuj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  default-password: Banking1234_
  